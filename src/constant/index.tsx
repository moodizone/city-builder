import { colors } from "@/utils/random";

export const MAX_FLOOR = 6;
export const MAX_ROOM = 4;

export const FALLBACK_FLOOR = 1;
export const FALLBACK_ROOM = 2;
export const FALLBACK_COLOR = colors[0];
